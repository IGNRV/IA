# Si usas el proxy de Vite, normalmente no necesitas esto.
# Alternativa: setear API base directo en el frontend.
VITE_API_BASE_URL=
# Solo si quieres cambiar el proxy target
VITE_API_PROXY_TARGET=http://localhost:8000
